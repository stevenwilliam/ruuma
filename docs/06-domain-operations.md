# Domain Operations — ruuma

**Version:** 0.1 (scaffold)
**Date:** 31 July 2026

The operational spine — the day-to-day workflows the product exists to run, and
the runbooks for when they go wrong. (In SCHOOLCATERING this was payments,
reconciliation, and kitchen operations; ruuma's equivalent goes here.)

---

## 1. Core workflows

> TODO(domain): each recurring operational workflow, step by step, with the
> actor, the trigger, the happy path, and the state it leaves behind.

## 2. Exception handling

> TODO(domain): what happens when each workflow fails — partial state, retries,
> manual overrides, who is notified.

## 3. Runbooks

> TODO(domain): the "it's broken at 9am, what do I do" procedures. Use `vi` in
> any shell examples.
